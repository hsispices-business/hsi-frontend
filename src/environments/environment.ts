import { AppConfig } from '../app/core/config/app-config.token';

export const environment: AppConfig = {
  production: true,
  apiBaseUrl: 'https://api.hsispices.com/v1',
  authUrl: 'https://auth.hsispices.com',
};
